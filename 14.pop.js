// pop() removes first element from that array and returns it

const numbers = [1, 2, 3];

numbers.pop();

console.log(numbers);  // [ 1, 2 ]

//---------------------------------------------------------------------------------------------

const numbers = [1, 2, 3];

numbers.pop();
numbers.pop();

console.log(numbers);  // [ 1 ]

//---------------------------------------------------------------------------------------------

const numbers = [1, 2, 3];

const lastItem = numbers.pop();

console.log(lastItem);  // 3

